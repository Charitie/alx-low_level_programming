#include "holberton.h"
/**
* add - adds two integers and returns the result.
* @i: integer to add
* @j: integer to add
* Return: Result
*/
int add(int i, int j)
{
int sum;

sum = (i + j);
return (sum);
}
